--- a/chrome/browser/extensions/external_provider_impl.cc
+++ b/chrome/browser/extensions/external_provider_impl.cc
@@ -854,6 +854,20 @@
         ManifestLocation::kExternalPrefDownload, Extension::NO_FLAGS);
     registry_provider->set_allow_updates(true);
     provider_list->push_back(std::move(registry_provider));
+
+    // Load bundled external extensions (e.g. Chromium Web Store) from the
+    // "extensions" directory next to chrome.dll, mirroring the behaviour of
+    // other desktop platforms. Auto-acknowledge so that bundled extensions
+    // install enabled without prompting the user.
+    auto bundled_pref_provider = std::make_unique<ExternalProviderImpl>(
+        service,
+        base::MakeRefCounted<ExternalPrefLoader>(
+            chrome::DIR_EXTERNAL_EXTENSIONS, ExternalPrefLoader::NONE,
+            nullptr),
+        profile, ManifestLocation::kExternalPref,
+        ManifestLocation::kExternalPrefDownload, Extension::NO_FLAGS);
+    bundled_pref_provider->set_auto_acknowledge(true);
+    provider_list->push_back(std::move(bundled_pref_provider));
 #else
     provider_list->push_back(std::make_unique<ExternalProviderImpl>(
         service,
