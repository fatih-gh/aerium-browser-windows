--- a/chrome/browser/resources/new_tab_page/new_tab_page.html
+++ b/chrome/browser/resources/new_tab_page/new_tab_page.html
@@ -1,35 +1,164 @@
 <!doctype html>
-<html dir="$i18n{textdirection}" lang="$i18n{language}">
-  <head>
-    <meta charset="utf-8">
-    <title>$i18n{title}</title>
-    <style>
-      body {
-        background: $i18n{backgroundColor};
-        margin: 0;
-      }
-
-      #backgroundImage {
-        border: none;
-        height: 100%;
-        pointer-events: none;
-        position: fixed;
-        top: 0;
-        visibility: hidden;
-        width: 100%;
-      }
-
-      [show-background-image] #backgroundImage {
-        visibility: visible;
-      }
-    </style>
-  </head>
-  <body>
-    <iframe id="backgroundImage" src="$i18nRaw{backgroundImageUrl}"></iframe>
-    <ntp-app></ntp-app>
-    <script type="module" src="new_tab_page.js"></script>
-    <link rel="stylesheet" href="chrome://resources/css/text_defaults_md.css">
-    <link rel="stylesheet" href="chrome://theme/colors.css?sets=ui,chrome">
-    <link rel="stylesheet" href="shared_vars.css">
-  </body>
+<html>
+<head>
+<meta charset="utf-8">
+<title>New Tab</title>
+<style>
+  :root {
+    --void:#080D1E; --deep:#0E1531; --surface:#151F3E; --surface-2:#1C284C;
+    --line:rgba(127,196,228,.14); --line-strong:rgba(127,196,228,.28);
+    --nebula:#4C97CF; --ice:#7FC4E4; --star:#EAF2FC; --dust:#8FA4C6;
+    --shadow:0 24px 60px -20px rgba(0,0,0,.65);
+    --stack:-apple-system,"Segoe UI",Roboto,system-ui,"Helvetica Neue",sans-serif;
+  }
+  * { box-sizing:border-box; margin:0; padding:0; }
+  html,body { height:100%; }
+  body {
+    font-family:var(--stack);
+    color:var(--star);
+    background:var(--void);
+    overflow:hidden;
+    -webkit-font-smoothing:antialiased;
+  }
+  .sky {
+    position:fixed; inset:0; z-index:0;
+    background:
+      radial-gradient(120% 90% at 50% -10%, #1a2a5e 0%, transparent 55%),
+      radial-gradient(80% 70% at 82% 12%, rgba(76,151,207,.22) 0%, transparent 50%),
+      radial-gradient(90% 80% at 12% 88%, rgba(94,72,150,.20) 0%, transparent 55%),
+      linear-gradient(180deg, var(--deep) 0%, var(--void) 100%);
+  }
+  #stars { position:fixed; inset:0; z-index:1; }
+  .vignette {
+    position:fixed; inset:0; z-index:2; pointer-events:none;
+    background:radial-gradient(120% 100% at 50% 42%, transparent 55%, rgba(4,7,18,.55) 100%);
+  }
+  main {
+    position:relative; z-index:3;
+    min-height:100%;
+    display:flex; flex-direction:column; align-items:center;
+    justify-content:center;
+    padding:48px 24px 40px;
+  }
+  .mark { opacity:.9; margin-bottom:26px; filter:drop-shadow(0 6px 18px rgba(76,151,207,.35)); }
+  .clock {
+    font-size:clamp(64px, 13vw, 148px);
+    font-weight:150;
+    letter-spacing:-.03em;
+    line-height:.9;
+    font-variant-numeric:tabular-nums;
+    color:var(--star);
+    display:flex; align-items:baseline; gap:.06em;
+  }
+  .clock .sep { color:var(--ice); font-weight:100; }
+  .clock .sec {
+    font-size:.26em; font-weight:300; letter-spacing:.02em;
+    color:var(--dust); align-self:flex-start; margin-top:.35em; margin-left:.15em;
+  }
+  .greeting {
+    margin-top:20px;
+    font-size:clamp(17px,2.4vw,22px);
+    font-weight:400; letter-spacing:.01em; color:var(--star);
+    text-wrap:balance; text-align:center;
+  }
+  .date {
+    margin-top:8px;
+    font-size:12.5px; font-weight:500; letter-spacing:.22em; text-transform:uppercase;
+    color:var(--dust);
+  }
+  form.search { margin-top:44px; width:min(560px,92vw); position:relative; }
+  .search input {
+    width:100%; height:58px;
+    padding:0 54px 0 52px;
+    border-radius:16px;
+    border:1px solid var(--line);
+    background:rgba(21,31,62,.72);
+    backdrop-filter:blur(14px); -webkit-backdrop-filter:blur(14px);
+    color:var(--star); font-family:var(--stack);
+    font-size:16px; letter-spacing:.01em;
+    box-shadow:var(--shadow);
+    transition:border-color .18s ease, box-shadow .18s ease, background .18s ease;
+    outline:none;
+  }
+  .search input::placeholder { color:var(--dust); }
+  .search input:focus {
+    border-color:var(--nebula);
+    background:rgba(24,35,68,.85);
+    box-shadow:var(--shadow), 0 0 0 4px rgba(76,151,207,.16);
+  }
+  .search .icon {
+    position:absolute; top:50%; transform:translateY(-50%);
+    left:19px; width:19px; height:19px; color:var(--dust); pointer-events:none;
+    transition:color .18s ease;
+  }
+  .search input:focus ~ .icon { color:var(--ice); }
+  .links {
+    margin-top:46px;
+    display:flex; flex-wrap:wrap; justify-content:center; gap:14px;
+    max-width:640px;
+  }
+  .link {
+    width:88px; display:flex; flex-direction:column; align-items:center; gap:9px;
+    text-decoration:none; cursor:pointer;
+    background:none; border:none; font-family:var(--stack);
+  }
+  .tile {
+    width:56px; height:56px; border-radius:18px;
+    display:grid; place-items:center;
+    background:linear-gradient(160deg, var(--surface-2), var(--surface));
+    border:1px solid var(--line);
+    font-size:21px; font-weight:500; color:var(--ice);
+    transition:transform .16s ease, border-color .16s ease, box-shadow .16s ease;
+    overflow:hidden;
+  }
+  .tile img { width:28px; height:28px; }
+  .link:hover .tile {
+    transform:translateY(-3px);
+    border-color:var(--line-strong);
+    box-shadow:0 12px 24px -12px rgba(76,151,207,.5);
+  }
+  .link:focus-visible { outline:none; }
+  .link:focus-visible .tile { border-color:var(--nebula); box-shadow:0 0 0 3px rgba(76,151,207,.3); }
+  .link .lbl { font-size:12px; color:var(--dust); letter-spacing:.02em; max-width:88px;
+    white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
+  .tile.add { color:var(--dust); font-weight:300; font-size:26px; background:transparent;
+    border-style:dashed; }
+  @media (prefers-reduced-motion:reduce) { * { transition:none !important; } }
+</style>
+</head>
+<body>
+
+<div class="sky"></div>
+<canvas id="stars"></canvas>
+<div class="vignette"></div>
+
+<main>
+  <svg class="mark" width="46" height="46" viewBox="0 0 512 512" aria-hidden="true">
+    <path d="M 330 384 L 149 489 A 256 256 0 0 1 108 47 L 108 256 A 148 148 0 0 0 330 384 Z" fill="#1B2C5E"/>
+    <path d="M 108 256 L 108 47 A 256 256 0 0 1 511 232 L 330 128 A 148 148 0 0 0 108 256 Z" fill="#2A4485"/>
+    <path d="M 330 128 L 511 232 A 256 256 0 0 1 149 489 L 330 384 A 148 148 0 0 0 330 128 Z" fill="#111C42"/>
+    <circle cx="256" cy="256" r="150" fill="#E9F1FB"/>
+    <circle cx="256" cy="256" r="104" fill="#2C6BAE"/>
+    <circle cx="238" cy="236" r="82" fill="#4C97CF"/>
+    <circle cx="222" cy="218" r="46" fill="#7FC4E4"/>
+  </svg>
+
+  <div class="clock" id="clock" aria-label="current time">
+    <span id="hh">00</span><span class="sep">:</span><span id="mm">00</span><span class="sec" id="ss">00</span>
+  </div>
+  <div class="greeting" id="greeting"></div>
+  <div class="date" id="date"></div>
+
+  <form class="search" id="searchForm" autocomplete="off">
+    <input id="q" type="text" placeholder="Search the web" aria-label="Search the web" />
+    <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
+      <circle cx="11" cy="11" r="7"/><path d="M21 21l-4.3-4.3"/>
+    </svg>
+  </form>
+
+  <div class="links" id="links"></div>
+</main>
+
+<script src="aerium_new_tab_page.js"></script>
+</body>
 </html>
--- /dev/null
+++ b/chrome/browser/resources/new_tab_page/aerium_new_tab_page.js
@@ -0,0 +1,87 @@
+  function pad(n){ return String(n).padStart(2,"0"); }
+  var months=["January","February","March","April","May","June","July","August","September","October","November","December"];
+  var days=["Sunday","Monday","Tuesday","Wednesday","Thursday","Friday","Saturday"];
+  function tick(){
+    var d=new Date();
+    document.getElementById("hh").textContent=pad(d.getHours());
+    document.getElementById("mm").textContent=pad(d.getMinutes());
+    document.getElementById("ss").textContent=pad(d.getSeconds());
+    var h=d.getHours();
+    var g = h<5?"Good night" : h<12?"Good morning" : h<18?"Good afternoon" : "Good evening";
+    document.getElementById("greeting").textContent=g;
+    document.getElementById("date").textContent=days[d.getDay()]+", "+d.getDate()+" "+months[d.getMonth()];
+  }
+  tick(); setInterval(tick,1000);
+
+  document.getElementById("searchForm").addEventListener("submit",function(e){
+    e.preventDefault();
+    var q=document.getElementById("q").value.trim();
+    if(!q) return;
+    var isUrl=/^(https?:\/\/|[\w-]+\.[\w-]{2,})/i.test(q) && !/\s/.test(q);
+    var url=isUrl ? (/^https?:\/\//i.test(q)?q:"https://"+q)
+                  : "https://duckduckgo.com/?q="+encodeURIComponent(q);
+    window.location.href=url;
+  });
+
+  var shortcuts=[
+    {label:"GitHub", mono:"G", url:"https://github.com"},
+    {label:"YouTube", mono:"Y", url:"https://youtube.com"},
+    {label:"Wikipedia", mono:"W", url:"https://wikipedia.org"},
+    {label:"Reddit", mono:"R", url:"https://reddit.com"},
+    {label:"Gmail", mono:"M", url:"https://mail.google.com"}
+  ];
+  var wrap=document.getElementById("links");
+  shortcuts.forEach(function(s){
+    var b=document.createElement("a");
+    b.className="link"; b.href=s.url;
+    b.innerHTML='<span class="tile">'+s.mono+'</span><span class="lbl">'+s.label+'</span>';
+    wrap.appendChild(b);
+  });
+
+  var canvas=document.getElementById("stars"), ctx=canvas.getContext("2d");
+  var reduce=window.matchMedia("(prefers-reduced-motion:reduce)").matches;
+  var stars=[], W=0, H=0, DPR=Math.min(window.devicePixelRatio||1,2);
+  function resize(){
+    W=canvas.width=innerWidth*DPR; H=canvas.height=innerHeight*DPR;
+    canvas.style.width=innerWidth+"px"; canvas.style.height=innerHeight+"px";
+    var count=Math.round(innerWidth*innerHeight/9000);
+    stars=[];
+    for(var i=0;i<count;i++){
+      stars.push({
+        x:Math.random()*W, y:Math.random()*H,
+        r:(Math.random()*1.3+0.3)*DPR,
+        base:Math.random()*0.5+0.25,
+        tw:Math.random()*Math.PI*2,
+        sp:Math.random()*0.6+0.2,
+        vx:(Math.random()-0.5)*0.04*DPR,
+        vy:(Math.random()*0.06+0.02)*DPR
+      });
+    }
+  }
+  function draw(t){
+    ctx.clearRect(0,0,W,H);
+    for(var i=0;i<stars.length;i++){
+      var s=stars[i];
+      var a=reduce? s.base : s.base + Math.sin(t*0.001*s.sp + s.tw)*0.28;
+      if(a<0.04) a=0.04;
+      var big=s.r>1.1*DPR;
+      ctx.beginPath();
+      ctx.arc(s.x,s.y,s.r,0,Math.PI*2);
+      ctx.fillStyle="rgba("+(big?"170,205,235":"220,232,248")+","+a.toFixed(3)+")";
+      ctx.fill();
+      if(big){
+        ctx.beginPath(); ctx.arc(s.x,s.y,s.r*2.6,0,Math.PI*2);
+        ctx.fillStyle="rgba(127,196,228,"+(a*0.14).toFixed(3)+")"; ctx.fill();
+      }
+      if(!reduce){
+        s.x+=s.vx; s.y+=s.vy;
+        if(s.y>H+4){ s.y=-4; s.x=Math.random()*W; }
+        if(s.x<-4)s.x=W; else if(s.x>W+4)s.x=0;
+      }
+    }
+    if(!reduce) requestAnimationFrame(draw);
+  }
+  resize(); addEventListener("resize",resize);
+  if(reduce) draw(0); else requestAnimationFrame(draw);
+
+  document.getElementById("q").focus();
--- a/chrome/browser/resources/new_tab_page/BUILD.gn
+++ b/chrome/browser/resources/new_tab_page/BUILD.gn
@@ -20,6 +20,7 @@
 
   static_files = [
     "new_tab_page.html",
+    "aerium_new_tab_page.js",
     "shared_vars.css",
   ]
 
